// Calendar Module
import calendar

date = input().split()
month, day, year = date

weekdays = calendar.day_name

weekday_num = calendar.weekday(int(year), int(month), int(day))

print(weekdays[weekday_num].upper())

// Time Delta
// TO SOLVE
